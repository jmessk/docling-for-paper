services:
  app:
    image: astral/uv:python3.12-bookworm-slim
    working_dir: /app
    volumes:
      - ./:/app
      - uv-cache:/root/.cache/uv
    command: uv run python main.py
    ports:
      - "8000:8000"

volumes:
  uv-cache:
